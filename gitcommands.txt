open git bash here 

git init
git status
git add -A
git commit -m "message"

this below is optional
git log   - to know the history
git checkout 'id copy'   a id shown in yellow colour
git checkout master


create a new repository in github 
copy the http path
git remote add anyname http path
git push -u anyname master (for first time we use -u)
git push anyname master 
git remote -v (to know the remote )


note
we have to put name for html file as index.html
